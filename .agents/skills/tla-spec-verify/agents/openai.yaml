interface:
  display_name: "TLA+ Spec Verify"
  short_description: "TLA+ modeling, TLC runs, and runner resolution"
  default_prompt: "Use $tla-spec-verify to resolve the component name, run the matching verify/ model and config, and record the TLC result."
