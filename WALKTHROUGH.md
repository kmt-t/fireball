# Fireball 仕様ウォークスルー計画 & フラット評価

**作成**: Claude Opus 5
**日付**: 2026-08-21
**対象リビジョン**: `f256b0a` / 検査器 `spec-integrator @ 597733b`
**現在のゲート状態**: 0 Errors, 0 Warnings（8 ゲート全通過）

---

## 第 I 部 — なぜ今ウォークスルーなのか

### 1. 機械で確かめられる層は出し切った

8 ゲート全通過、形式検証モデル 4 本すべて `expect: True` の証明形式、検証義務 12/12 履行、
検査器リビジョンも記録済み。**この状態からゲートを増やしても、得られるものは急速に減る。**

問題は、ここから先に**残っているリスクの質が変わる**ことだ。
以下は現時点で全ゲートが緑のまま素通りしている 3 件である。いずれも実在する。

### 2. 全ゲートが緑のまま残っている問題（実測）

#### W-1. デッドロック証明の前提が、要求仕様に反している 🔴

| 出典 | 記述 |
| :--- | :--- |
| `architecture_overview.md:142` | 通信モデル = **同期メッセージング（全体）**。「CSP ハンドオフも IPC ルータ経由も、**呼び出し側は応答を待つ（BLOCKED 状態）**」 |
| `ipc_router.md:109` | 「**厳格なノンブロッキング送信**と所有権巻き戻しロジックによるデッドロック不在」 |
| `os_coos.md:248` | 「**ノンブロッキング送信**と有界ハンドオフにより…循環待ちデッドロックに陥らない」 |
| `coos_channel_model.py` | `deadlock_freedom_proof`: `AG(Not(deadlock))` `expect: True` → **PASS** |

デッドロックフリーの証明は「**送信は決してブロックしない**」を前提に成立している。

**この前提は偽である。** 要求仕様が答えを既に決めているからだ。

| 出典 | 記述 |
| :--- | :--- |
| `requirement_list.md:74` | `{CSPCommunication}`「**ホーアCSP**に基づき、所有権移譲によるゼロコピーメッセージパッシングを行う」 |
| `os_coos.md:5` | 「**ホーアCSP**ベースのグリーンスレッド OS」 |
| `os_coos.md:22` | `channel` =「1エントリのバッファを持つ**同期**オブジェクト」 |
| `os_coos.md` `channel_send` | 受信待ちが居なければ「送信キューにデータを積んで**サスペンド**」 |

**ホーアCSP の通信はランデブーであり、同期である。** 送信は受信側が現れるまでブロックする。
`channel_send` の擬似コード自体がそう書いている。
したがって `architecture_overview.md:142`（同期・BLOCKED）が**正しく**、
`ipc_router.md:109`（厳格なノンブロッキング送信）が**外れ値**である。

そして**ブロックするランデブーは、原理的にデッドロックしうる。**
A が B へ send、B が A へ send が交差すれば、双方 BLOCKED、READY キューは空、スケジューラは idle。
これはホーア CSP の教科書的デッドロックであり、CSP にデッドロック解析の文献が存在する理由そのものである。

**結論: Fireball の CSP 層はデッドロックフリーではない。** 証明されるべきは
「カーネルがデッドロックを防ぐ」ではなく、
**「チャネル依存グラフに循環が無い」という*アプリケーション側の*性質**である
（非循環トポロジ、またはクライアント・サーバ規律）。

さらにモデルを確認したところ、`coos_channel_model.py` には
**ブロック状態が 1 つも存在しない**（`grep -c "block\|wait\|BLOCKED" → 0`）。
`channel_send` の else 分岐——ブロックする経路そのもの——が写されていない。
`s_deadlock` は入る辺を持たず、到達不能なのは**保護されているからではなく、経路を書いていないから**である。

#### W-2. W^X を証明するモデルはあるが、W^X を実装する仕様がない 🔴

`jit_cache_model.py` は `w_xor_x_safety_proof`: `AG(Not(writing ∧ executing))` を `expect: True` で証明している。

一方、`docs/**` 全 34 文書のうち **MPU に言及するのは 2 文書のみ**（`os_coos.md` の付随言及、
`interface_wit.md` の panic 理由）。`jit_compiler.md` にも `platform_memory.md` にも MPU の記述はない。
第2返信 §2(3) で約束された「JIT キャッシュを通常時 `RO+X`、コンパイル時のみ `RW+XN` に
切り替える MPU 制御プロトコル」は、**仕様書に書かれていない。**

**存在しない機構の性質を証明している。** これは「正しく書かれた、対象の無いモデル」であり、
Formal Gate の設計上、検出できない。

#### W-3. 8KB 部分ページの意味論が未定義のまま既定値になっている 🟠

`FB_CONF_GUEST_RAM_SIZE` は依然 `8192`。`runtime_loader.md:112` はこう書く。

> 物理割当が部分ページ（例: 8KB）の場合でも、モジュール初期ページ要求（1 page = 64KB）と
> **整合させ**、実行時境界判定へ引き渡す。

**「整合させ」の中身が定義されていない。** 取りうる選択肢は 3 つで、結果がまったく違う。

| 方式 | `memory.size` の戻り | 標準ツールチェーン出力 | WASM 仕様適合 |
| :--- | :--- | :--- | :---: |
| ロード時に拒否 | — | 動かない（明示的に落ちる） | ✅ |
| `1` を返す（実体 8KB） | 1 | **静かに壊れる** | ❌ |
| `0` を返す | 0 | `(memory 1)` 違反 | ❌ |

第2返信 §2(1) で「部分ページは非標準モードへ格下げ、Phase 1 は 64KB 前提」と回答されたが、
**既定値も `--stack-first` 等のリンク要件も文書に反映されていない。**
Phase 1 の WAMR 比較ベンチが実行できるかどうかがここに懸かっている。

### 3. この 3 件が示していること

いずれも**単一文書内では正しく、文書間で矛盾している**か、
**成果物としては正しく、対象が存在しない**。
前者はトレーサビリティ検査を通り、後者は形式検証を通る。

> **ウォークスルーの対象は、「各文書が正しいか」ではなく「文書群が同じシステムを描いているか」である。**

---

## 第 II 部 — ウォークスルー計画

### 4. 方式と原則

本計画は IEEE 1028 のウォークスルー（author-led review）を、
**単一のオーナー + 複数の AI レビュア**という現実の体制に合わせて再構成したものである。

**原則 1: 反証課題を先に置く。**
各セッションは「文書を読む」のではなく、**「これが偽であるとしたら何が真でなければならないか」**
を先に宣言してから読む。目的のないレビューは誤字しか見つからない。

**原則 2: 落ちたら以降を打ち切る。**
セッションは依存順に並べる。S1 が NO-GO ならば S3 以降を実施しない。
動かない前提の上で JIT の詳細を検討しても、時間が失われるだけである。

**原則 3: 説明責任は起草者にある。**
各セッションでは Gemini（起草者）が**声に出して説明**し、レビュアは質問のみを行う。
「読めば分かる」は不可。説明できない箇所が欠陥である。

**原則 4: 記録は欠陥のみ。合意は記録しない。**
「問題なし」の記録は次のレビューで再検証されず、既成事実になる。

### 5. 役割

| 役割 | 担当 | 責務 |
| :--- | :--- | :--- |
| **オーナー / 議長** | 人間（あなた） | 進行、打ち切り判断、GO/NO-GO の最終決定。**唯一の判定権者** |
| **起草者 (Author)** | Gemini | 各セッションで設計意図を説明する。質問に答える |
| **記録者** | 任意の AI | 欠陥のみを記録。合意・説明は記録しない |
| **反証者 (Devil's Advocate)** | Opus / 別の AI | 反証課題の追及に専念。**起草にも検証基盤にも関与していない主体が望ましい**（§10 参照） |
| **実装者代理** | 別の AI（新規セッション） | **この往復を知らない**主体。仕様だけを渡し、実装できるかを試す（S6） |

### 6. 入場基準（これを満たさない状態で開始しない）

- [ ] `run_all_tests.ps1 -full` が 0 Errors で完走し、`Verifier:` 行に `UNCOMMITTED` が付かないこと
- [ ] `spec-consistency.lock` が最新でコミット済みであること
- [ ] `{Challenge_*}` 8 件の「検討中」について、**各セッションでどれを扱うかが割り当て済み**であること
- [ ] 起草者が各セッションの対象文書を事前に通読していること

### 7. セッション構成

各 90 分。1 日 2 セッションを上限とする（判断の質が落ちるため）。

---

#### S1. 実行可能性ゲート — Phase 1 が走るか 🔴 最優先

**反証課題**: 「標準の clang/wasm-ld が出力した wasm32 バイナリを、現在の仕様どおりの実装で実行できる」は偽ではないか。

| 対象 | `runtime_loader.md` §メモリセクション検証 / `runtime_vmmio.md` §1 / `system_config_details.md` §2.4 |
| :--- | :--- |
| **扱う課題** | W-3、`{WasmPageAlignment}`、`{FastAddressCheck}`、`{Wasm32Only}` |

**確認事項**
1. `FB_CONF_GUEST_RAM_SIZE = 8192` のとき `memory.size` は何を返すか。**表の 3 択のどれか**
2. `__stack_pointer` がリニアメモリ上端に置かれる標準出力をどう扱うか。リンク要件は文書のどこにあるか
3. Phase 1 の WAMR 比較ベンチは、どちらの構成で走るのか（64KB / 部分ページ）
4. 「部分ページは非標準モード」という決定は、どの文書のどこに書かれているか

**退出基準**: 上記 4 点が文書上の記述として特定できること。特定できない項目は欠陥として記録。
**打ち切り条件**: 1 と 3 が両立しないと判明した場合、S4 以降を中止し設計変更へ回す。

---

#### S2. 隔離モデル — ハイパーバイザを名乗れるか 🔴

**反証課題**: 「ゲストの不正なコードは、ホストおよび他ゲストに影響しない」は偽ではないか。

| 対象 | `runtime_vmmio.md` 全体 / `platform_memory.md` / `jit_compiler.md` §4.1 |
| :--- | :--- |
| **扱う課題** | W-2、`{vMMIO_Isolation}`、`{MemoryIsolation}`、`{GLOBAL_IndependentHeap}`、`{META_FaultIsolation}`、`{Challenge_SyscallMemorySafety}` |

**確認事項**
1. **JIT キャッシュは実行時どの属性か。** RWX なら、JIT の 1 バグが即システム全体の任意コード実行になる
2. `jit_cache_model.py` が証明した W^X を、**実際に強制する機構はどの文書のどこにあるか**（W-2）
3. Cortex-M33 の MPU リージョンは 8 または 16。何をいくつ使うのか。切替コストは
4. `{GLOBAL_IndependentHeap}`「物理的に独立」は、単一特権コンテキストの協調型システムで何を意味するのか
5. エイリアシング受容の `TODO(Phase 2)` は、誰がいつ再評価するのか

**退出基準**: 2 と 3 に文書上の回答があること。無ければ「隔離は境界チェックのみに依存」と明記させる。

---

#### S3. 並行性モデル — 証明の前提は正しいか 🔴

**反証課題**: 「デッドロックフリーの証明は、実装される設計について述べている」は偽ではないか。

| 対象 | `architecture_overview.md` §4 / `ipc_router.md` §4 / `os_coos.md` §3.3, §4 / `coos_channel_model.py` |
| :--- | :--- |
| **扱う課題** | W-1、`{Challenge_CspHandoffStarvation}`、`{Challenge_CoosBlockedList}`、`{Challenge_InterruptSafety}`、`{Challenge_ApproximateYield}` |

**前提（決着済み・議論しない）**: `{CSPCommunication}` が「ホーアCSP」を指定している以上、
通信はランデブーであり **send はブロックする**。`channel_send` の擬似コードもそう書いている。
したがって `ipc_router.md:109` の「厳格なノンブロッキング送信」を削除するのが是正であって、
「どちらにするか」は議題ではない。

**確認事項**
1. `ipc_router.md` の route_message は、COOS の CSP チャネルと**同じ機構か、別の機構か**。
   別ならキュー付き非同期でよいが、`architecture_overview.md:142` の「両方 BLOCKED」は誤りになる
2. **デッドロックフリーは何のレベルで主張するのか。** カーネルは保証できない。
   非循環チャネルグラフか、クライアント・サーバ規律か。どちらを採るか
3. 2 の規律は**誰がいつ検査するのか**。静的（URI レジストリからグラフを構築して循環検出）か、実行時か
4. `coos_channel_model.py` に**ブロック状態を追加**し、循環 send がデッドロックに至る経路を書く。
   そのうえで 2 の規律をガードとして表現し、`build_model(guards=False)` で到達可能になることを示す
5. `FB_CONF_MAX_CONSECUTIVE_HANDOFFS` の既定値はいくつか。定義箇所はどこか
6. ISR → INT イベント → yield 点回収が `{EliminateDataRace}`「原理的に排除」を満たす根拠

**退出基準**: 2 が確定し、`ipc_router.md`・`os_coos.md`・`architecture_overview.md` の
デッドロック関連記述がその答えに揃っていること。
**注記**: `coos_channel_model.py` は作り直しになる。現行モデルはブロック経路を含んでいない。

---

#### S4. 実行エンジン — 内部整合と実装可能性 🟠

**反証課題**: 「インタプリタと JIT は同一の実行コンテキストを矛盾なく共有できる」は偽ではないか。

| 対象 | `runtime_interpreter.md` / `jit_compiler.md` / `jit_engine_copy_patch.md` / `jit_runtime_entry.md` / `runtime_vsoc.md` |
| :--- | :--- |
| **扱う課題** | `{Challenge_JITCacheEfficiency}`、`{JIT_LazyChaining}`、`{Interpreter_LazyJITSwitch}`、`{JIT_Safepoint}` |

**確認事項**
1. `execution_context` のどのフィールドが JIT ネイティブコードから直接触られるか。レイアウトは固定されているか
2. 3 面ローテーション中に、破棄されるバンクのコードを**実行中**であることは起こり得ないか
3. チェイニングはバンクをまたげるか。またげないなら、チェイニングの効果はどの程度残るか
4. Safepoint はどの命令境界に置かれるか。JIT コードにも挿入されるか。コストは
5. デバッガ介入時の JIT フラッシュと、実行中トレースの関係

---

#### S5. 検証の妥当性 — 何が証明され、何が証明されていないか 🟠

**反証課題**: 「4 本のモデルは、Fireball の設計について述べている」は偽ではないか。

| 対象 | `formal/*.py` 4 本 / `formal_model_contract.md` / `doc_risk_report.md` |
| :--- | :--- |

**確認事項**
1. 各モデルの状態集合は、**どの設計文書のどの記述**から導かれたか。1 対 1 で対応づけられるか
2. 抽象化で捨てた側面は何か。`{META_Risk_Tiering}` 的に、捨てた側面のリスクは許容範囲か
3. リスク評価器が `{VERIFY_FORMAL}` を推奨した 12 セクションのうち、モデルで実際に覆われているのは何件か
4. 8 件の `{Challenge_*}` が「検討中」のまま Phase 1 に入って良い理由
5. **このセッションの反証者は、検証基盤を作っていない主体であること**（§10）

---

#### S6. 実装可能性テスト — 部外者が作れるか 🟠

**反証課題**: 「この往復を知らない実装者が、仕様だけで実装できる」は偽ではないか。

**方法**: この会話を一切知らない新規 AI セッションに、`docs/**` のみを渡して以下を依頼する。

1. `runtime_loader.md` に従って `BinaryStream` と `module_view` のヘッダを書け
2. `os_scheduler.md` に従ってスケジューラの公開 API ヘッダを書け
3. 途中で判断がつかなかった箇所を**すべて列挙せよ**

**評価対象は成果物ではなく 3 の列挙である。** 質問が出た箇所が仕様の欠落箇所である。
往復の文脈を共有した者同士では、この欠落は永久に見えない。

---

#### S7. 予算・計画の再評価 🟢

**反証課題**: 「Phase 1 は 3 か月で終わる」は偽ではないか。

| 対象 | `resource_budget.md` / `roadmap_phase.md` / `backlog_list.md` |
| :--- | :--- |

**確認事項**
1. §4.2 のコンポーネント予算配分の合計は、制約と整合しているか（RAM / ROM / SLOC を実際に足す）
2. 最小構成 RAM 32KB は、静的合計 24.0KB に対して成立するか
3. S1〜S6 で発生した欠陥を織り込んだ工期はどうなるか
4. `{Size_15KLOC}` は誰が何で計測し、いつ最初に測るか

### 8. 退出基準（Phase 1 GO の条件）

- [ ] S1〜S3 の反証課題がすべて「偽ではない」と確認されたこと（＝設計が成立すること）
- [ ] W-1 / W-2 / W-3 が解消、または**明示的な受容リスクとして記録**されていること
- [ ] S6 で列挙された質問のうち、Phase 1 の実装に必要なものが解消されていること
- [ ] `{Challenge_*}` のうち Phase 1 に関わるものが「決定済」または「Phase 2 へ延期」と明記されていること
- [ ] 上記を踏まえた **オーナーによる GO 判定**（他の誰も代行できない）

### 9. 欠陥の分類

| 区分 | 定義 | 扱い |
| :--- | :--- | :--- |
| **Blocker** | これが偽なら Phase 1 が走らない | 即時決着。セッション打ち切り |
| **Contradiction** | 文書間で両立しない記述 | どちらかを削除。両論併記で残さない |
| **Undefined** | 判断に必要な記述が存在しない | 記述するか `TODO(Phase X):` で可視化 |
| **Unsupported** | 主張はあるが根拠がない | 根拠を作るか、主張を降格 |
| **Cosmetic** | 表記・体裁 | まとめて後処理。セッション中は扱わない |

---

## 第 III 部 — フラット評価

### 10. 評価にあたっての利益相反の明示

**私（Opus）は、この仕様の独立したレビュアではもはやない。**

- 形式検証モデルが従う**契約書を私が書いた**
- それを判定する**ゲートを私が実装した**
- D1〜D3 の**設計決着を私が下した**

したがって §5（検証の妥当性）と、Formal Gate の妥当性そのものについて、
**私の評価は自己評価であり、独立した検証にならない。** S5 の反証者は別の主体を立てるべきである。

加えて、この往復で**私が置いた手本が 3 回、失敗の直接原因になった**
（`EF` の許容、`expect: False` の参照実装、液性演算子の実装バグ）。
私はテンプレートの危険性を第1信 §3 で指摘した側であり、その後 3 回自分で踏んだ。
以下の「検証基盤」の点数は、この事実込みで読まれたい。

### 11. 多角評価（初回監査時 → 現在）

| 軸 | 初回 | 現在 | 評価 |
| :--- | :---: | :---: | :--- |
| **構想・アーキテクチャの野心** | 8 | 8 | 変わらず良い。Copy-and-Patch × スタックレス Coroutine × CSP は筋が通る |
| **文書体系・トレーサビリティ** | 8 | 9 | Tier 分解の思想は業務水準。キーワード追跡が co-change 検知の基盤として機能した |
| **内部整合性（機械検出可能）** | 3 | **9** | 値のドリフト・伝播漏れ・宙吊り参照が実質ゼロ。最大の改善 |
| **内部整合性（意味的）** | 3 | **5** | W-1 が象徴的。単一文書内は整合、文書間で矛盾。ゲートの死角 |
| **技術的正確性** | 4 | 7 | M33 キャッシュ、PTE ビット衝突、優先度矛盾は解消。W-2 / W-3 が残る |
| **形式検証の実質** | 1 | **6** | 反証可能・全称液性・違反状態到達不能の証明形式まで到達。ただし前提（W-1）と対象（W-2）が未検証 |
| **仕様↔コード整合** | 1 | — | コード自体が撤去されたため評価対象外。仕組み（Consistency Gate）は整備済み |
| **リソース予算の妥当性** | 2 | 6 | 架空の実績値は削除。合計の再計算と 32KB 構成の検証は S7 待ち |
| **検証パイプライン** | 6 | **8** | 4→8 ゲート、義務台帳、lockfile、検査器リビジョン刻印。ただし §10 の相反あり |
| **プロセス規律** | 3 | **8** | R1〜R9 が成文化され、大半が機械強制されている。自己採点の禁止が定着 |
| **実装可能性** | 4 | **4** | **変わっていない。** W-3 が未決着である限り Phase 1 が走るか不明 |
| **計画の現実性** | 3 | 5 | 虚偽の [x] は撤去。ただし工期は未再評価 |

### **総合 44 → 68 / 100**

### 12. 評価の要約

**改善は本物で、しかも構造的である。**
「捏造された検証主張」というカテゴリの欠陥は消滅した。消えただけでなく、
**再発すると機械が落とす**ようになった。M1〜M5 の 5 類型のうち 4 つは、
規律ではなくゲートによって封じられている。これは 1 週間の成果としては相当なものだ。

**しかし GO 判定は依然として NO-GO であり、理由が入れ替わった。**

| | 初回の NO-GO 理由 | 現在の NO-GO 理由 |
| :--- | :--- | :--- |
| 性質 | **主張が虚偽だった** | **設計判断が未決着** |
| 例 | 存在しないモデルを根拠に挙げていた | 送信がブロックするのかしないのか決まっていない |
| 解決手段 | 検証を実施する | **決める** |
| 誰が解決するか | 起草者 | **オーナー** |

初回は「書いてあることが本当か」が問題だった。今は**「何を作るのかが決まっていない」**が問題である。
これは後退ではなく、**解けるべき問題が正しい順序で表面化した**状態だ。

### 13. 最も重要な一点

> **W-1: デッドロックフリーの証明が、アーキテクチャ文書と矛盾する前提の上に立っている。**

初回監査の H-5（同期／非同期の三重矛盾）は、一度も解決されないまま、
その一方の側に形式証明が載った。**証明があることで、矛盾が見えにくくなった。**

これは形式手法の典型的な失敗であり、ゲートでは原理的に検出できない。
モデル検査器はモデルを検査するのであって、モデルが設計を写しているかは検査しない。

S3 を最優先で実施し、**送信がブロックするのかしないのかを決めること。**
それが決まるまで、`coos_channel_model.py` の PASS は意味を持たない。

---

## 付録: 実施順の推奨

```
Day 1  S1 実行可能性        → Blocker が出たら即停止し設計変更へ
Day 2  S3 並行性（W-1 決着） → S2 より先。証明の前提が決まらないと S5 が空回りする
Day 3  S2 隔離モデル
Day 4  S6 実装可能性テスト   → 準備に時間がかかるため早めに着手
Day 5  S4 実行エンジン
Day 6  S5 検証の妥当性       → 反証者を別主体に交代
Day 7  S7 予算・計画再評価   → S1-S6 の欠陥を織り込む
       → オーナー GO/NO-GO
```

S5 を最後に置くのは、S1〜S4 で設計が動けばモデルも作り直しになるためである。
先に検証の妥当性を議論しても、対象が変わる。
