# Fireball 設計複雑度 & リスク評価レポート (Risk Assessment Report)

- **評価セクション総数**: 15
- **形式検証 (pyModelChecking) 推奨セクション**: 12
- **LLM 意味監査 推奨セクション**: 1

---

## 1. 形式検証 (pyModelChecking) が推奨される重要セクション

| ファイル | セクション | 複雑度 | リスク | 推奨検証 | 推奨タグ | 主なリスク要因 |
| :--- | :--- | :---: | :---: | :--- | :--- | :--- |
| `components/tier1_core/os_coos.md` | **6.1 検証対象の不変条件** | 5/5 | **5/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | High risk of deadlock and starvation in CSP handoff mechanisms during asynchronous state transitions.<br>C++20 coroutine suspension/resumption introduces non-trivial state space complexity regarding liveness (AF properties). |
| `components/tier1_core/os_coos.md` | **4.1 アルゴリズム** | 4/5 | **5/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | CSP Handoff and Direct Context Switch bypass the scheduler, creating non-standard state transitions that can lead to priority inversion or stack corruption if not atomically managed.<br>Race conditions between Interrupt Wakeup (ISR context) and Direct Context Switch (Task context) regarding the ownership of the task state (READY vs RUNNING). |
| `components/tier1_core/os_coos.md` | **1. コンセプト** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Potential for deadlocks inherent in CSP-based synchronous communication channels<br>Complexity of ensuring zero-copy ownership transfer invariants across coroutine suspension points |
| `components/tier1_core/os_coos.md` | **3.1 データ構造** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Potential for deadlocks in the 1-entry synchronous 'channel' buffer during producer-consumer handoffs.<br>Ownership transfer risks in 'co_value' that could lead to use-after-free or double-free if not strictly managed. |
| `components/tier1_core/os_coos.md` | **4.2 状態遷移図 (SMD: COOS システムレベル)** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Direct Context Switch (CSP Handoff) bypasses standard scheduler queues, creating potential for priority inversion or deadlock if ownership transfer fails.<br>The transition from Idle/RunningTask to Recovery via memory faults requires strict invariant checking to ensure the Recovery state cannot be corrupted by the same fault that triggered it. |
| `components/tier1_core/os_scheduler.md` | **1. コンセプト** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Potential for deadlocks in CSP-based message passing if circular dependencies occur between coroutines.<br>Risk of starvation or priority inversion in a cooperative multitasking environment without a preemptive fallback. |
| `components/tier1_core/os_scheduler.md` | **3.1 データ構造** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | State transition complexity between READY, BLOCKED, and RUNNING states in a coroutine-based scheduler.<br>Potential for priority inversion or task starvation during execution order control. |
| `components/tier1_core/os_scheduler.md` | **4.2 状態遷移図 (SysML SMD: Scheduler 視点)** | 4/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | CSP Handoff bypasses the standard scheduler loop via direct resume(), creating a high risk of stack corruption or inconsistent scheduler state if not atomically managed.<br>Potential for deadlock in CSPWait transitions where both sender and receiver might enter wait states due to race conditions during the 'opposite ready' check. |
| `components/tier1_core/os_scheduler.md` | **3.2 内部ブロック図** | 3/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Potential for race conditions between the Scheduler Engine and the State Visualizer Interface when accessing TCB (Task Control Block).<br>Critical dependency on interrupt_controller and timer_driver timing for deterministic task switching. |
| `components/tier1_core/os_scheduler.md` | **4.1 アルゴリズム** | 3/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Race conditions between the Round-Robin scheduler and asynchronous interrupt-driven task wakeups (InterruptWakeup).<br>Potential for priority inversion or starvation if the 'priority resumption' logic for interrupts overrides the Round-Robin fairness without a defined preemption policy. |
| `components/tier1_core/os_scheduler.md` | **ADR-SCHED-003: BLOCKEDタスクリストの管理コストとリアルタイム性** | 3/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Race conditions between the event-driven notification (interrupt context) and the scheduler's task state transitions.<br>Potential for priority inversion or starvation if the wake-up queue does not strictly adhere to priority-based scheduling. |
| `components/tier1_core/system_config_details.md` | **2.4 vSoC / vMMIO {VERIFY_LLM}** | 3/5 | **4/5** | `pyModelChecking` | `{VERIFY_FORMAL}` | Potential for memory corruption if FB_CONF_GUEST_RAM_SIZE (8KB) is smaller than FB_CONF_WASM_PAGE_SIZE (64KB) without strict bounds checking.<br>Race conditions and synchronization errors in the JIT Triple-Buffer cache promotion logic (JIT_OldestOnly_Promote). |

---

## 2. 全セクションの複雑度・リスク評価一覧 (降順)

| ファイル | セクション | Tier | 複雑度 | リスク | 推奨手法 | 推奨タグ | 評価サマリー |
| :--- | :--- | :---: | :---: | :---: | :--- | :--- | :--- |
| `components/tier1_core/os_coos.md` | 6.1 検証対象の不変条件 | 1 | 5/5 | 5/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The specification describes a core OS synchronization primitive using CSP handoffs and C++20 coroutines. The combination of asynchronous suspension and blocking communication creates a high-dimensional state space where deadlocks and starvation are critical risks. Mathematical proof via CTL/LTL model checking is mandatory to guarantee liveness and safety invariants. |
| `components/tier1_core/os_coos.md` | 4.1 アルゴリズム | 1 | 4/5 | 5/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The specification describes a high-performance OS core with non-standard scheduling bypasses (Direct Context Switch/CSP Handoff). The bypass of the central scheduler significantly increases the state space complexity and introduces critical risks regarding concurrency, atomicity, and liveness. Mathematical verification via Kripke structures/LTL is required to prove the absence of deadlocks and ensure that the system always returns to a schedulable state. |
| `components/tier1_core/os_coos.md` | 1. コンセプト | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The system implements a Hoare CSP-based green thread OS using C++20 coroutines. While it aims to eliminate data races via ownership transfer, the reliance on synchronous message passing and cooperative scheduling introduces significant risks of deadlock and liveness failures. Mathematical verification of the state transitions and channel invariants via LTL/CTL is required. |
| `components/tier1_core/os_coos.md` | 3.1 データ構造 | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The specification describes a CSP-based concurrency model with strict memory ownership. The combination of synchronous channels and ownership transfer creates a high-risk state space prone to deadlocks and race conditions, necessitating formal verification of liveness and safety properties via LTL/CTL. |
| `components/tier1_core/os_coos.md` | 4.2 状態遷移図 (SMD: COOS システムレベル) | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The system introduces high-risk concurrency patterns, specifically 'Direct Context Switching' and 'CSP Handoff', which bypass traditional scheduling. These mechanisms, combined with strict memory isolation and a recovery state machine, create a state space where race conditions and deadlocks are non-trivial to detect via testing. Mathematical verification of LTL/CTL properties is required to ensure liveness and safety during task handoffs and fault recovery. |
| `components/tier1_core/os_scheduler.md` | 1. コンセプト | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The design implements a high-complexity stackless scheduler using C++20 coroutines and CSP. The combination of cooperative multitasking and zero-copy communication introduces significant risks regarding liveness (deadlock/starvation) and state consistency, necessitating formal verification of the state transition model via LTL/CTL. |
| `components/tier1_core/os_scheduler.md` | 3.1 データ構造 | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | While the section describes data structures, these structures govern the core state machine of the OS scheduler. The use of coroutines and explicit state management (READY/BLOCKED/RUNNING) introduces a significant state space where deadlocks and illegal state transitions are possible, necessitating formal LTL/CTL verification of the scheduler's liveness and safety properties. |
| `components/tier1_core/os_scheduler.md` | 4.2 状態遷移図 (SysML SMD: Scheduler 視点) | 1 | 4/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The design implements a complex CSP-style concurrency model with a high-risk 'Direct Handoff' optimization. The bypass of the central scheduler for low-latency switching introduces non-trivial state space transitions that are prone to race conditions and deadlocks, necessitating formal verification via LTL/CTL to prove safety and liveness invariants. |
| `components/tier1_core/os_scheduler.md` | 3.2 内部ブロック図 | 1 | 3/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | While the block diagram is structurally simple, it describes the core OS scheduler. The interaction between the Engine, TCB, and asynchronous hardware drivers introduces significant concurrency risks that require LTL/CTL verification to ensure liveness and safety invariants. |
| `components/tier1_core/os_scheduler.md` | 4.1 アルゴリズム | 1 | 3/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | While the Round-Robin logic is simple (O(1)), the interaction between the idle detection state and asynchronous interrupt wakeups introduces critical concurrency risks. Formal verification via LTL/CTL is required to prove liveness (no deadlock) and safety (no missed wakeups) during state transitions. |
| `components/tier1_core/os_scheduler.md` | ADR-SCHED-003: BLOCKEDタスクリストの管理コストとリアルタイム性 | 1 | 3/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | The transition from linear scanning to an event-driven O(1) wake-up mechanism introduces critical concurrency challenges. Since this is a Tier 1 OS scheduler component, the correctness of state transitions (BLOCKED -> READY) under asynchronous interrupts must be mathematically proven to ensure deterministic real-time behavior and absence of deadlocks. |
| `components/tier1_core/system_config_details.md` | 2.4 vSoC / vMMIO {VERIFY_LLM} | 1 | 3/5 | 4/5 | `pyModelChecking` | `{VERIFY_FORMAL}` | While the section appears to be a simple configuration table, the underlying logic it governs—specifically the JIT triple-buffering state machine and the discrepancy between WASM page size and physical RAM allocation—introduces significant memory safety and concurrency risks. Formal verification of the buffer promotion state transitions and memory boundary invariants is required. |
| `components/tier1_core/os_scheduler.md` | ADR-SCHED-002: アルゴリズムの継続的改善と最適化 {VERIFY_LLM} | 1 | 2/5 | 2/5 | `LLM_Judge` | `{VERIFY_LLM}` | This section defines a structural architectural decision (separation of concerns) rather than a concrete implementation of a scheduling algorithm. The complexity is low as it focuses on API decoupling. The primary risk is semantic alignment between the interface and future implementations, making LLM-based semantic auditing the appropriate verification method. |
| `components/tier1_core/system_config_details.md` | 2.7 型定義・予約値 | 1 | 1/5 | 2/5 | `Static` | - | This section is purely declarative, defining the scope and purpose of the task_id type. It contains no state transitions, concurrency logic, or complex algorithms, making it suitable for static analysis and type-checking rather than formal model checking. |
| `components/tier1_core/system_config.md` | 4.1 アルゴリズム | 1 | 1/5 | 1/5 | `Static` | - | The section describes a purely static configuration component with no dynamic behavior, state transitions, or runtime algorithms. It consists solely of compile-time constants, posing negligible architectural risk. |